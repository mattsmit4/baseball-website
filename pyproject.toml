[project]
name = "softball-rotation"
version = "0.1.0"
description = "Pickup softball defensive rotation web app"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
pythonpath = ["."]
asyncio_mode = "auto"
